export default function HomePage() {
  return (
    <main className="min-h-screen flex items-center justify-center p-8">
      <section className="max-w-2xl w-full rounded-2xl border border-gray-200 bg-white p-10 shadow-sm">
        <p className="text-xs font-semibold uppercase tracking-widest text-gray-500">SIBOC</p>
        <h1 className="mt-3 text-4xl font-black text-gray-900">New Website Template</h1>
        <p className="mt-4 text-gray-600 leading-relaxed">
          This repo is ready with Next.js, TypeScript, and Tailwind CSS.
          We can now build the full SIBOC website here.
        </p>
      </section>
    </main>
  );
}
